<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        Book Crud
    </title>

    <!-- bootstrap css -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body style="color: white; background-color: #1d2630;">
    <div class="container mt-5">
        <div class="text-center">
            <h1 class="display-5 mb-5">
                <strong>
                    // Edit Book //
                </strong>
            </h1>
        </div>
        <div class="main row justify-content-center">
            <form action="/addBook" method="post" class="row justify-content-center mb-4" autocomplete="off">
                <input type="text" name="editId" value="<%= singleBook.id %>" hidden />
                <div class="col-8 mb-3">
                    <label>Book Name :</label>
                    <input class="form-control" type="text" name="bookname" placeholder="Enter Book Name"
                        value="<%= singleBook.bookName %>" />
                </div>
                <div class="col-8 mb-3">
                    <label>Type :</label>
                    <input class="form-control" type="text" name="type" placeholder="Enter Book Type"
                        value="<%= singleBook.type %>" />
                </div>
                <div class="col-8 mb-3">
                    <label>Author :</label>
                    <input class="form-control" type="text" name="author" placeholder="Enter Book Author"
                        value="<%= singleBook.author %>" />
                </div>
                <div class="col-8 mb-3">
                    <label>Price :</label>
                    <input class="form-control" type="number" name="price" placeholder="Enter Book Price"
                        value="<%= singleBook.price %>" />
                </div>
                <div class="col-8">
                    <input class="btn btn-success add-btn" type="submit" value="Update Book" />
                </div>
            </form>
        </div>
    </div>

</body>

</html>